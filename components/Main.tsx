import { Stack } from "@chakra-ui/react";

export const Main = (props) => (
  <Stack
    spacing="2rem"
    width="100%"
    maxWidth="50rem"
    mt="-45vh"
    pt="15rem"
    px="1rem"
    {...props}
  />
);
